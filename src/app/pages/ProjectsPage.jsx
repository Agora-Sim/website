/* ============================================================
   0. IMPORTS
   ============================================================ */

import { ProjectIndex } from '@/features/project-index';

/* ============================================================
   1. COMPONENT
   ============================================================ */

/** The `/projetos` page: the full register, one section for now. */
export default function ProjectsPage() {
  return <ProjectIndex />;
}
